#!/usr/bin/env bash

set -euo pipefail

GH_REPO="https://github.com/ktsopanakis/redmine-tui"

list_all_versions() {
  git ls-remote --tags --refs "$GH_REPO" |
    grep -o 'refs/tags/.*' |
    cut -d/ -f3- |
    sed 's/^v//' |
    sort -V
}

list_all_versions
